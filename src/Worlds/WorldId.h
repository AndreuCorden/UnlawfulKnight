#ifndef WORLDID_H
#define WORLDID_H

enum class WorldId {
    World0,      // Starting farm / field area
    World1,      // First town / tournament grounds
    GlobalMap    // Strategic overview / realm map
};

#endif // WORLDID_H